import { EelVersion } from "./types.js";
type CompilerOptions = {
    pools: {
        [name: string]: Set<string>;
    };
    functions: {
        [name: string]: {
            pool: string;
            code: string;
        };
    };
    eelVersion?: EelVersion;
    preParsed?: boolean;
};
export declare function compileModule({ pools, functions: funcs, eelVersion, preParsed, }: CompilerOptions): Uint8Array<ArrayBuffer>;
export {};
