import { EelVersion } from "./types";
type LoadModuleOptions = {
    pools: {
        [name: string]: {
            [name: string]: WebAssembly.Global;
        };
    };
    functions: {
        [name: string]: {
            pool: string;
            code: string;
        };
    };
    eelVersion?: EelVersion;
};
export declare function loadModule({ pools, functions, eelVersion, }: LoadModuleOptions): Promise<WebAssembly.Instance>;
export {};
